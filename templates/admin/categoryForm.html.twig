{% extends 'baseAdmin.html.twig' %}

{% block title %}machine{% endblock %}

{% block body %}
{{ form_start(categoryForm) }}
        <div class="row">
            <div class="col-6">
                {{ form_row(categoryForm.title,{'label': 'titre'})}}
            </div>
            <div class="col-6"> 
                {{ form_row(categoryForm.description , {'label':'Description'}) }}
            </div>
        </div>
        <div class="row">
            <div class="col-12"> 
                {{ form_row(categoryForm.img, {'label':'photo'}) }}
            </div>
        
        </div>  

        <div class="col-12 d-flex justify-content-end">
            <a href="{{path('admin_category_index')}}" class="btn btn-danger m-3">Annuler</a>
            <button class="btn btn-success ">Créer</button>
        </div>


    {{ form_end(categoryForm) }}

{% endblock %}