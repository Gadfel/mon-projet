{% extends 'baseAdmin.html.twig' %}

{% block title %}Address{% endblock %}

{% block body %}

        <h1> liste des Address</h1>
        <table class="table text-center table-hover table-sm">
            <thead>
                <tr>
                    <th>N°</th>
                    <th>Nom</th>
                    <th>address1</th>
                    <th>address2</th>
                    <th>code postal</th>
                    <th>ville</th>
                    <th>pays</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                
                {% for user in  users %}
                    <tr>
                        <td class="align-middle">{{ loop.index }}</td>
                        <td class="align-middle">{{ address.Name }}</td>
                        <td class="align-middle">{{ address.address1 }}</td>
                        <td class="align-middle">{{ address.address2 }}</td>
                        <td class="align-middle">{{ address.postalCode }}</td>
                        <td class="align-middle">{{ address.city }}</td>
                        <td class="align-middle">{{ address.country }}</td>
                        <td class="align-middle">
                        {# <a href="{{ path('user_update',{'id':address.id}) }}" class="text-primary"><i class="fas fa-pen-square"></i></a> #}
                            <a href="{{ path('address_delete', {'id': address.id}) }}" class="text-danger"><i class="fas fa-trash-alt"></i></a> 
                        </td>
                    </tr>
                {% endfor %}
                
            </tbody>
        </table>
        <div class="d-flex justify-content-end my-5">
            <a href="{{ path('address_create') }}" class="btn btn-success"> Ajouter une   d'un utilisateur</a>
        </div>
{% endblock %}