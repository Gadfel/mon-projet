{% extends 'base1.html.twig' %}

{% block title %}Aprovisionnement !{% endblock %}

{% block body %}

     <h1 class="text-center">Nos Produits</h1>

     <div class="row">
          {% for product in products %}
            {% if id is same as (product.category.id ) %}
                <div class="col-12 col-md-3 col-xl-4 ">
                    <div class="card">
                        <img src="{{ asset('images/produits/'~ product.img)}}" class="img-fluid" alt="aprovisionnement">
                            <div class="card1-body text-center">
                                 <h5 class="card-title2">{{product.name}}  <br> {#{{ product.quantity}} #}<br> {{ product.price|number_format(2,',', '')}} €  TTC</h5>
                                <p class="card-text">{{ product.description}}</p>
                                <a href="{{ path('cart_add', {'id': product.id}) }}"  class="btn-1">Ajouter au panier</a>
                            </div>
                    </div>
                </div>
            {% endif %}
          {% endfor %}
      </div>
      {% endblock %}
  


